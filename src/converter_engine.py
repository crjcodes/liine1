class ConverterEngine:
  """
  Responsible for taking input data and mapping it to our internal model
  Currently, this is a CSV of data, which will be mapped to 
  {TODO}
  """
  def ConvertFrom(filename):
    """
    Takes the name of a csv file matching the business specification, then
    maps to a list of our internal data model
    """
    return False